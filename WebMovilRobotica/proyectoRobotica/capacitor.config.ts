import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyectoRobotica',
  webDir: 'www/browser'
};

export default config;
